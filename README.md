# Startup Pitch Bot

A Streamlit app to help founders refine their startup pitch.

## Setup

1. Install dependencies:
```bash
pip install -r requirements.txt
```

2. Run the app:
```bash
streamlit run app.py
```

## Development

The main application is in [app.py](app.py).
